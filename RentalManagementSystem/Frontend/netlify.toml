[build]
  base = "RentalManagementSystem/Frontend"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"

# Environment variables (set these in Netlify UI)
# VITE_API_URL = "https://your-cloud-run-url.run.app"
